import { IconLoading } from "@/components/icons/loading"

export default function Loading() {
  return (
    <main className="fixed inset-0 flex items-center justify-center">
      <IconLoading className="h-12 w-12" />
    </main>
  )
}
